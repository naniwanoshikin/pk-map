<%# ul に li を追加 %>
$('#notify_lists').append("<%= escape_javascript(render @notifications) %>");

<%# 現在の'もっと見る'を削除 %>
$("#view_more").replaceWith("<%= escape_javascript(link_to_next_page(@notifications, 'もっと見る', remote: true, id: 'view_more')) %>");

<%# classつける %>
$('#view_more').addClass('btn btn-primary');
