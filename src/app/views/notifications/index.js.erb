<%# ul に li を追加 %>
$('#notify_lists').append("<%= escape_javascript(render @notifications) %>");

<%# a要素 [もっと見る]を[もっと見る]に置換 理解がイマイチ.. %>
$("#view_more a").replaceWith("<%= escape_javascript(link_to_next_page(@notifications, 'もっと見る', remote: true)) %>");

<%# classつける %>
$('#view_more a').addClass('btn btn-border');
