<%# (static_pages/home) %>
<% if @feed_items.any? %>
  <ol class="posts">
    <%# StaticPages(C) %>
    <%= render @feed_items %>
    <%# (posts/_post) %>
  </ol>
  <%= paginate @feed_items,
    remote: true, # ajax
    params: { controller: :static_pages, action: :home }
  %>
<% end %>
