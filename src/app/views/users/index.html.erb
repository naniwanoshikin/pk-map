<%# 10 %>
<% provide(:title, 'All users') %>
<h2>ユーザー一覧</h2>

<%# ajax %>
<div id="ajax_users_index">
  <ul class="users">
    <%# (users/_user) %>
    <%= render @users %>
  </ul>
  <%= paginate @users, remote: true %>
</div>
